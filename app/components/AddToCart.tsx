'use client';
import React from 'react'

const AddToCart = () => {
    return (
        <div>
            <button className='btn btn-primary' onClick={() => window.alert("button clicked!!!")}>Add to cart</button>
        </div>
    )
}

export default AddToCart